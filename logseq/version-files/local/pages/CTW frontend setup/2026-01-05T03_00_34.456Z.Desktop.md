type:: [[Project]]
parent:: [[CTW]]
deadline:: [[Jan 13th, 2026]] 
status:: active

- query-sort-by:: block
  query-table:: true
  query-sort-desc:: true
  #+BEGIN_QUERY
  {:title "🗃️ All Tasks"
  :query [:find (pull ?b [*])
         :where
         ;; 1. 匹配任务状态 (LATER, NOW, DONE)
         [?b :block/marker ?marker]
         [(contains? #{"LATER" "NOW" "DONE"} ?marker)]
  
         ;; 2. 匹配页面名称及其别名 (这里定义你要查的页面)
         [?p :block/name ?pagename]
         [(contains? #{"ctw frontend setup"} ?pagename)]
  
         ;; 3. 路径引用匹配 (含父级继承)
         [?b :block/path-refs ?p]]
  ;; 注意：移除了 :inputs 和所有时间相关的 where 条件
  :collapsed? false}
  #+END_QUERY