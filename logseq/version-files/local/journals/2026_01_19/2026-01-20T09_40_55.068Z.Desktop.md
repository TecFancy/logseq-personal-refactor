## 💼 Work Stream
- 09:15 [[CTW]] BPM-A(Team2) daily standup #meeting
- [[CTW Frontend Setup]]
	- LATER [[CTW]] [AI] assign Agent Skills Configuration task to [[Phoenix]] ([Use Agent Skills in VS Code](https://code.visualstudio.com/docs/copilot/customization/agent-skills)) #coding
	  DEADLINE: <2026-01-27 Tue 17:30>
	- LATER [[CTW]] [AI] assign MCP Server Configuration task to [[Phoenix]] #coding
	- LATER [[CTW]] [[Phoenix]] set up UT environment #coding
	- LATER [[CTW]] [AI] complete Feature-Based project rules and #coding
	- DONE [[CTW]] implement ESLint #coding
	  :LOGBOOK:
	  CLOCK: [2026-01-19 Mon 11:10:57]--[2026-01-19 Mon 13:54:24] =>  02:43:27
	  :END:
	- DONE [[CTW]] **Architecture & Documentation:** Established strict architectural guidelines and documentation for hooks, libraries, and component layers, including new decision trees and naming conventions. #writing
		- **完善架构文档与规范定义**
		- 明确了 `src/hooks` 与 `src/lib` 的职责边界（如禁止在 lib 中使用 Hooks），新增了代码放置的决策树与命名规范；同时为 `app`、`pages`、`types` 和 `store` 等目录补充了详细的 README，确立了组合层（Composition Layer）与展示层的界限。
	- DONE [[CTW]] **Enforcement of Boundaries:** Integrated `eslint-plugin-boundaries` and TypeScript ESLint to programmatically enforce architectural constraints and prevent illegal cross-layer dependencies.
	  :LOGBOOK:
	  CLOCK: [2026-01-19 Mon 17:20:13]--[2026-01-19 Mon 17:20:14] =>  00:00:01
	  :END:
		- **落地架构约束与 Lint 工具链**
		- 引入了 `eslint-plugin-boundaries` 和 `typescript-eslint`，配置了严格的模块依赖规则（如禁止跨特性依赖、禁止全局组件引用 Store），确保架构设计在代码层面得到强制执行。
	- DONE [[CTW]] **Routing & Pages:** Restructured the application routing system by introducing seven new business pages, a 404 error page, and a separate route section for testing.
		- **重构路由体系与新增业务页面**
		- 重组了应用路由结构（区分主应用与 `/test` 路径），新增了 404 页面及 `SelfInitiationPage` 等 7 个核心业务页面，并清理了 `vite.config` 中冗余的路径别名（aliases）。
	- DONE [[CTW]] **Refactoring & Cleanup:** Refactored the `AppLayout` structure and cleaned up the codebase by removing unused path aliases, timer components, and redundant legacy code.
		- **优化应用布局与代码清理**
		- 将 `AppLayout` 抽离并规范了目录命名（`resizable-sider`），修复了 Tree 组件类型安全问题；同时移除了未使用的 Timer 组件及相关测试代码，保持代码库整洁。
	- DONE [[CTW]] **Configuration & Assets:** Updated project configuration and assets, including PWA icons, VSCode settings, and global type definitions for a better development experience.
		- **基础设施与配置更新**
		- 更新了 PWA 资源（Favicon/Icons）、VSCode LiveServer 配置，并增加了开发环境的 `debugLog` 工具函数及 Document Picture-in-Picture 类型定义。
- LATER 2025 年项目任务产出，及系统优化方面的成果 #writing
  DEADLINE: <2026-01-15 Thu 17:30>
	- 参考[Gemini](https://gemini.google.com/share/09d0f41341eb)的方法梳理
-
-
-
-
-
-
-
-
-
-
-
-
-
-
-
- ---
- ## 🏠 Personal & Growth
-